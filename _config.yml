# This is the default format. 
# For more see: http://jekyllrb.com/docs/permalinks/
#permalink: /:categories/:year/:month/:day/:title 
permalink: /:year/:month/:day/:title

# exclude: [".rvmrc", ".rbenv-version", "README.md", "Rakefile", "changelog.md"]
# Themes are encouraged to use these universal variables 
# so be sure to set them if your theme uses them.
#
title : 五十亩东坡地
email: wanlong.gao@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  若言琴上有琴声，
  放在匣中何不鸣？
  若言声在指头上，
  何不于君指上听？
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://blog.allenx.org" # the base hostname & protocol for your site

# Sidebar filter
# Choose 'tag' or 'category' as filter in sidebar.
#
filter: 'tag'

# Build settings
highlighter: pygments
markdown: redcarpet  # [ maruku | rdiscount | kramdown | redcarpet ]
markdown_ext:  markdown,mkd,mkdn,md
textile_ext:   textile
redcarpet:
  extensions:
    - fenced_code_blocks
    - no_intra_emphasis
    - strikethrough
    - no_intra_emphasis
    - autolink
    - tables
    - with_toc_data

sass:
  style: compressed
gems:
  - jekyll-sitemap
# The production_url is only used when full-domain names are needed
# such as sitemap.txt 
# Most places will/should use BASE_PATH to make the urls
#
# If you have set a CNAME (pages.github.com) set your custom domain here.
# Else if you are pushing to username.github.io, replace with your username.
# Finally if you are pushing to a GitHub project page, include the project name at the end.
#
production_url : http://blog.allenx.org

# Social account
# twitter:  username
weibo:    lengyuex
github:   gaowanlong
# codepen:  username
# dribbble: username


# Comment
disqus_shortname: allenx


# Google Analytics
# google_analytics: tracking_ID
